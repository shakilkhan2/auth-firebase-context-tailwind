/**
 * Context API:
 * 1. Create Context and export it;
 * 2. Create Provider;
 * 3. Set Context Value;
 * 4. to access the value: useContext hook
 **/ 


/**
 * 1. set AuthProviders on main.jsx;
 * 2. inside provider access children props and the use
 **/ 


/**
 * 1. breakdown the create user function
 * 2. auth provider will have auth and pass the email and password from register
 * **/ 